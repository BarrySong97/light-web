import React, { FC } from 'react';
import * as ReactDOM from 'react-dom';

export interface ChartsProps {}

const Charts: FC<ChartsProps> = () => {
  return <div className="mountNode"></div>;
};

export default Charts;
